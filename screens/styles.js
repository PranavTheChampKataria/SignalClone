import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    inputContainer: { //only work on react-native-elements
        fontSize: 20,
        padding: 15,
        width:250,
    },
   button:{ //only work on react-native-elements
    width:200,
    marginTop:10,
   },
    
});

export default styles;